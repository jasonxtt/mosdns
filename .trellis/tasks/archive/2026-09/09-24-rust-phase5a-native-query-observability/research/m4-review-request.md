+REVIEW_ONLY — prospective M4 bounded helper-GC calibration, same current task/user-authorized measurement correction, same designated002reviewer.
Repo /Users/tom/github/mosdns-rust origin jasonxtt/mosdns branch rust. Exact pushed parent c6d3466e753bae22def4da41447d1fd1c9c43502 -> head 2a56052cd9ef868eb4873964c3c38ad97f84173e. Compare https://github.com/jasonxtt/mosdns/compare/c6d3466e753bae22def4da41447d1fd1c9c43502...2a56052cd9ef868eb4873964c3c38ad97f84173e .
Your M3 protocol PASS authorized fixed controls. M3 ran its two W1 batches once:18 exits0,36 valid primary rows,270000 correct-on-time; both batches repeated identical-binary TCP200 p99 guard,12 comparisons with individual latency crossings, only1/8 equivalence intervals qualifies. It stopped prospectively before W2/W3. Raw431 files/84MiB verified, selected102 files verified, assessment/results committed in parent. M3 remains unqualified, M2 remains unqualified, V12 remains failed, no A5.
M4 scope in task research/measurement-revision-v4.md: single prospective intervention to Go test processes sharingCPU1: GOGC=off, GODEBUG=gctrace=1, GOMEMLIMIT removed, preserve GOMAXPROCS1. Rust SUT/binary/source unchanged; existing services untouched. Per-query allocations/defaultGC are plausible noise, no causal claim. Original25sec primary200/400 traffic, socket/latency/deadline/counter/event/TTL semantics and old individual guards plus fixed10% six-pair log-equivalence unchanged. W2 independent-prefilled originalTTL. Recovery indeterminate.
Preflight >=2GiB MemAvailable, finite25sec stages/fresh terminated helper processes. Offline post-attempt collector hashes generator/verifier+fixture stderr, counts GC traces, derives sampled Go-role RSS peaks from all resource files. Missing logs/roles, any trace, rolepeak>256MiB rejects qualification; cap is sampled gate not hard allocatorlimit. Complete raw retained. Runner enforces+records GC profile; qualifier checks evidence. GCdisabled increases Go harnessmemory; no Rust RSS/CPU claim.
Same frozen two27 plans; run18 W1 slots first, fail stops before rest; if fixed W1 gate qualifies only, run remaining36 slots then full exact126-primary/28-interval gate. No retries/replacements/filtering/margin widening/candidate data or old-result reinterpretation. Fresh measurement-v4/results-m4-calibration roots, driver pins tool/input hashes. No M4 measured attempts exist.
Changed paths: scripts/run-phase5a-baseline.sh; task implement.md; task research/{measurement-revision-v4.md,run-m4-calibration.sh,qualify-m2-controls.py,qualify-m4-controls.py,summarize-m4-gc.py,test_m4_measurement.py}. ExistingM2/M3 defaults/remote archives preserved. Full helper tests and37 Python regression/synthetic tests passed Linux+macOS; shell syntax and staged diffchecks pass. Linux tools staged only, no measured traffic.
Read-only exact range/protocol alignment review. Return explicit FINAL: PASS or FINAL: FAIL with stable scoped IDs. PASS permits only fixed M4 controls; cannot award A5/V12 or authorize candidate protocol. No edits, benchmarks, agents, new tasks, lifecycle/archive/deploy or threshold changes. Identify out-of-scope concerns separately as major blockers.
