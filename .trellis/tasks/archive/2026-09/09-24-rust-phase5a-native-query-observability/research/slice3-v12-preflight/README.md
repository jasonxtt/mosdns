# V12 preflight evidence

This folder is a direct capture of the V12 preflight-only result directory on
`mosdns-rust`. The candidate binary and helper v8, source manifest, frozen
runner and fixtures, CPU sets, audit overlays, and ext4 result storage passed.
`attempt-order.tsv` contains only its header; no benchmark attempt ran during
preflight. The UTC timestamp is recorded in `results-v12-preflight/run-audit.txt`.
