{"file":".trellis/spec/backend/rust-migration.md","reason":"Phase 3B+ pure Rust boundary and phase-gate policy: new modules compose as Rust crates for the future host, add no Go adapter/selector/fallback/ABI, and each module needs its own approved task with a frozen contract/deviation matrix and rollback. Directly governs the reuse owner's placement in rust/upstream-core and the 'no Go/cgo/FFI, no second runtime' constraint."}
{"file":".trellis/spec/backend/quality-guidelines.md","reason":"Verification and review-gate rules the executor must follow: RED-before-behavior, focused-then-full checks, warnings-denied clippy, preserve unrelated dirty files, exact-path staging, and the external ChatGPT root-review loop with explicit PASS/FAIL. Also defines dispatch granularity (Herdr one active-task assignment; slices are internal milestones)."}
{"file":".trellis/tasks/09-18-rust-phase4-connection-reuse-pipeline/design.md","reason":"Normative technical design for this task: ReuseKey/SecureKey composition, the settled serial-per-connection policy (design.md section 7) that keeps original DNS IDs untouched, deadline/cancel/close invariants, failure classification limited to SideEffectState::NotSent, the confirmed non-configurable task-local bounds (section 6: MAX_IDLE_PER_KEY=1, MAX_IDLE_TOTAL=8, IDLE_TIMEOUT=10s, MAX_PENDING_PER_CONNECTION=1, no wait queue), and the compatibility/deferral matrices the implementation and checker must both enforce."}
{"file":".trellis/tasks/09-18-rust-phase4-connection-reuse-pipeline/research/reuse-pipeline-evidence.md","reason":"Source-backed evidence for the contracts being extended and the behavior deliberately not copied: exact lib.rs/tcp.rs/secure line references for Lifecycle, ExchangeContext, SideEffectState, framing, and identity separation; Go characterization (pipelineConcurrentLimit=64, idleTimeout 10s/30s) as implementation-only per the archived upstream-foundation matrix; and the reuse key facts for resolver/owner.rs:857-938."}
