# V11 preflight evidence

This folder is a direct capture of the V11 preflight-only result directory on
`mosdns-rust`, with no benchmark attempts. The frozen source, candidate binary,
helper v8, runner, inputs, CPU-set check, and ext4 result storage passed before
the official matrix. `attempt-order.tsv` contains only the header. The preflight
UTC timestamp is recorded in `results-v11-preflight/run-audit.txt`.
