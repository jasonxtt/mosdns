[package]
name = "mosdns-runtime"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[lib]
name = "mosdns_runtime"
crate-type = ["staticlib", "rlib"]

[dependencies]
mosdns-cache-core = { path = "../cache-core" }
mosdns-matcher-core = { path = "../matcher-core" }

[lints]
workspace = true
