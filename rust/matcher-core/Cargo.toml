[package]
name = "mosdns-matcher-core"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[lib]
name = "mosdns_matcher_core"
crate-type = ["rlib"]

[dependencies]
regex = { version = "=1.11.1", features = ["perf"] }

[lints]
workspace = true
